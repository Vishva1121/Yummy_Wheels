<%- include('partials/header') %>

<h1>
    <%= restaurantType %>
</h1>

<% restaurants.forEach(restaurant => { %>
    <h3><%= restaurant.name%></h3>
    <img src="/uploads/<%= restaurant.img %>" alt="">
<% }); %>

<%- include('partials/footer') %>